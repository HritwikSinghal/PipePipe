# Project: PipePipe+ — DeArrow support & signed release fork

> Last updated: 2026-06-01 | Session: 14

## Overview
Maintain a personal fork of **PipePipe** (a NewPipe-based Android client) that adds **DeArrow** support — crowdsourced de-clickbait **titles + thumbnails** for YouTube — while tracking upstream `InfinityLoop1308/PipePipe`. The PipePipe repo is a thin meta-repo; real code lives in submodules (`PipePipeClient` = the app, `PipePipeExtractor` = the extractor lib). Strategy: **fork only what we modify** — only `PipePipeClient` is forked (to `HritwikSinghal/PipePipeClient`) and carries a `patch` branch; the extractor stays upstream-pinned/dormant. The meta-repo's `patch` branch repoints the client submodule at our fork and is the **default branch** on origin. End state: `git fetch upstream` + rebase keeps us current, `nix run .#build` produces a signed **PipePipe+** APK (distinct `applicationId`, installs alongside the official app), and a `workflow_dispatch` GitHub Actions release publishes a signed APK (keystore via GitHub Secrets) — mirroring `~/Projects/reddit/continuum`.

## Current State (verified from repo, 2026-06-01, Session 14)
- **Meta-repo** `patch` = **22 commits** atop `main` (= upstream v5.1.1 `989e9fd`): Nix flake + submodule wiring, the signed-release pipeline (`release.yml`), the running series of DeArrow gitlink bumps, and docs. Clean, **0 ahead of `origin/patch`** (pushed). `main` = clean upstream mirror (`989e9fd`). Default branch on origin = **`patch`**. Remotes: `origin`=`HritwikSinghal/PipePipe`, `upstream`=`InfinityLoop1308/PipePipe`.
- **Submodule pins** (meta `patch` tree): `PipePipeClient` @ `bdbedb742` (our fork, branch `patch`), `PipePipeExtractor` @ `871ea2d` (upstream, dormant), `PipePipe.wiki` @ `40f284d` (never initialized). `.gitmodules`: client → fork `patch`; extractor + wiki → upstream.
- **PipePipeClient** `patch` = **14 commits** atop upstream/dev `188c64eb9`, HEAD `bdbedb742`. Clean, **0 ahead of `origin/patch`** (pushed). Holds the full DeArrow feature (titles + thumbnails + interactive badge across every stream surface), the perf/reliability hardening (Sessions 10–13), and the universal-APK + Gradle build tuning (Session 14) — full commit list in Decisions. Remotes: `origin`=fork, `upstream`=`InfinityLoop1308`. Default branch on fork = **`patch`**. **Upstream/dev tip is still `188c64eb9` → no upstream drift; client `patch` is current.**
- **PipePipeExtractor**: detached @ `871ea2d`, upstream-only, **dormant** (DeArrow is client-only; its fork was deleted Session 3 — no unique work).
- **Backups:** `backup/pre-squash-s4` exists in both repos (pre-squash tips); client also has `patch-prerebase-s3` (pre Session-3 upstream rebase). Safe to delete once the rebased history is confirmed good.
- **Build/release:** `nix run .#build` → signed PipePipe+ universal APK; `.#debug` for fast iteration; `workflow_dispatch` release pipeline live and previously verified on-device via Obtainium. Phases 1–7 done; **only Phase 8 (upstream-sync maintenance + docs) remains.**

## Next Session Pickup (START HERE)

**Status (Session 14 — build-time improvements IMPLEMENTED + verified + shipped; resume here):**
Cut CI + local build cost via a **single universal APK** + Gradle parallel/cache tuning. Full detail
in Decisions & Notes (2026-06-01, Session 14). **Verified:** `:app:assembleDebug -PforkAbiFilter=universal`
→ BUILD SUCCESSFUL, config-cache clean (0 problems), exactly **1 APK** `PipePipe_5.1.1-universal-debug.apk`
(contains `arm64-v8a`+`x86_64` = the old universal's content; ffmpeg-kit ships only those two ABIs, so
dropping armeabi-v7a/x86 loses nothing — those split APKs had no ffmpeg native lib); dearrow suite
**48/48**. **Committed + pushed** (client `patch` then meta `patch` per the gotcha) and a release run
was triggered (`gh workflow run release.yml`). **REMAINING:** confirm the release run is green + the
published universal APK installs/updates via Obtainium. --- _Prior:_

**Status (Session 13 — DeArrow `/bugs` hunt: High+Medium fixes IMPLEMENTED + verified, Low deferred; resume here):**
Bug-hunted the whole DeArrow implementation (bug-finder agent over `util/dearrow/` + all wiring
sites; findings verified against source) and fixed the 4 High/Medium issues via 4 parallel
subagents in isolated `PipePipeClient` submodule git worktrees (disjoint file sets, consolidated by
patch into the main tree). **Uncommitted** on client `patch`, **13 files**: **H1** — `Player.destroy()`
now calls `deArrowController.dispose()` (it was the only title site never disposed → RxJava + view
leak + late write to a torn-down player); **M2** — holders/adapters/Groupie `StreamItem` now dispose
in-flight DeArrow fetches on recycle/unbind (`disposeDeArrow()` on the 4 stream holders +
`onViewRecycled` in `InfoListAdapter`/`LocalItemListAdapter` + `unbind` in `StreamItem.kt`); **M3** —
`DeArrowService.bucket()` cache/in-flight TOCTOU closed (re-check cache inside the atomic
`computeIfAbsent`, self-removing `Maybe.just` placeholder); **M4** — missing `randomTime` now →
keep-original (parser default `0.0`→`Double.NaN` + selector NaN guard; was a black `time=0` frame).
**Verified:** `:app:assembleDebug` BUILD SUCCESSFUL + dearrow unit suite **48/48** (was 46; +2 M4
regression tests). **REMAINING: commit** — push client `patch` (13 files) **BEFORE** the meta gitlink
bump (standing gotcha); on-device re-check pending (user's release+Obtainium flow). 4 Low findings
(L1–L4) documented under "Deferred / To verify" for later. --- _Prior:_

**Status (Session 12 — DeArrow perf/reliability fix IMPLEMENTED + on-device VERIFIED; resume here):**
The Session-11 root cause (over-fetch → 5xx storm → 6h poison-cache) is fixed. Implemented all 3
approved parts in the client (working tree, **uncommitted**): (a) `DeArrowService.fetchBucket` now
caches **only 404** as "no data" — 5xx/429/network/timeout are surfaced as `IOException`, NOT cached;
(b) `DeArrowPrefetcher` caps each page to the **first 25 items** (`MAX_PREFETCH_ITEMS`) and drops
`MAX_CONCURRENCY` **4→2**; (c) `fetchBucket` does a **bounded transient-only retry** (`retryWhen`,
2 attempts, 500ms→1000ms backoff). Rebuilt `nix run .#debug`, reinstalled on the Pixel 10a,
re-captured `DeArrowPerf`. **Verified (before → after):** 5xx rate ~28% → **~9.5%**; negative-cached
buckets **44 → 0**; fetch latency median **7.8 s → 0.4 s** (89% → 20% >3 s); per-row resolve median
**~35 ms** (655 warm HITs); retry recovered **44/45** transient 5xx (1 gave up, uncached). Full
before/after table + honest note on the remaining server-side tail: **`docs/dearrow-perf-investigation.md`
→ "Verification (Session 12)"**. Compile clean; dearrow unit suite **46/46** (no regression).
**REMAINING: commit** — client `patch` (the 3-part fix; `DeArrowService.java` + `DeArrowPrefetcher.java`)
**BEFORE** the meta gitlink bump (standing gotcha); decide whether to keep the `DeArrowPerf`
instrumentation long-term or strip it. Optional follow-ups if the tail still annoys: shorter
per-request timeout, persistent disk cache, honor `Retry-After`/global backoff. --- _Prior
(Session 11, investigation):_ diagnosed via `BuildConfig.DEBUG`-gated `DeArrowPerf` instrumentation
(committed client `fffdfda99`) + a `release.yml` "PipePipe Debug" APK step; the Session-10 "prefetch"
change was itself the overload source (RC1). --- _Prior:_

**Status (Session 8 update):** **Phase 5 is now CODE-COMPLETE (6/7)** — DeArrow thumbnails + the always-present interactive toggle badge are implemented (unified `DeArrowItemController`), recomposed into 3 logical commits (`a860ce2a4..754992090`) and force-pushed to `origin/patch`; dearrow unit tests 40/40. The only open Phase-5 item is on-device verification (gated on Phase 6/7), and a clean full `nix run .#build` was NOT re-run this session (per-task `assembleDebug` + unit tests all passed). **Phase 6 + 7 are now DONE** — the signed release pipeline is live and a test release (`PipePipe+ 5.1.1-plus.1`, tag `plus-v5.1.1-plus.1`) is published with 5 signed per-ABI APKs. On-device verified (Session 8): the DeArrow titles/thumbnails/badge work and `PipePipe+` installs alongside the official app. `nix run .#build` now builds the **signed release** (`.#debug` added for fast iteration). **Phases 1-7 are all DONE. Next = Phase 8 (upstream-sync maintenance + docs)** — script the `git fetch upstream` + rebase + re-pin flow, add maintenance notes. --- _Prior status:_ **Phase 1 build WORKS** (`nix run .#build` → 5 debug APKs; Session 6 JDK swap). **Phase 4 is code-complete (5/6)** — units A/B/C + the new "Mark replaced titles" indicator are all committed & pushed (client `patch` HEAD `d6e6bcaa8`; meta gitlink bumped). The only open Phase-4 item is **manual on-device verification, which the user will do via the GitHub release pipeline + Obtainium** — so it's gated on **Phase 6 (signing + distinct applicationId) and Phase 7 (release workflow)**. **Next = Phase 5 — but the user EXPANDED its scope (Session 6): the marker becomes interactive + always-present and must toggle title AND thumbnail. DESIGN (brainstorm) BEFORE coding; it revises the committed Phase-4 marker. See the "Phase 5 scope EXPANDED" note below for the full requirements + design tensions.** (Phase 6/7 for a signed APK can come before or after.) (Build verified each step: assembleDebug + dearrow unit tests 23/23. `runCheckstyle` is NOT a gate — upstream fails it with 2192 errors and CI doesn't run it; our dearrow files add 0 errors / 1 unavoidable override warning.)

**Unit C — what Session 5 changed (uncommitted, in `PipePipeClient` working tree):**
- `DeArrowTitleApplier`: added `dispose()` (for transient sites) + a `url != null` gate (closes a latent NPE — `getId(null)` throws `NullPointerException`, which `apply()`'s catch did not cover).
- `VideoDetailFragment.java`: applier field + `apply(detailVideoTitleView, serviceId, url)` at both `showLoading()` (~:1810) and `handleResult()` (~:1842). TextView-only, `title` field untouched. [OK]
- `Player.java`: applier field + `apply(titleTextView, info.getServiceId(), info.getUrl())` at `onMetadataChanged` (~:3536).
- `InfoItemDialog.java`: local applier + `apply` after the title set (:60); disposed via `dialog.setOnDismissListener(... dispose())` (transient view — avoids retaining a dismissed dialog).
- `PlayQueueActivity.java`: applier field + `apply(songName, ...)` in `onMetadataUpdate` (~:494) — the now-playing title.
- **`PlayQueueItemHolder` + `PlayQueueItemBuilder`**: per-holder applier field; `apply` in `buildStreamInfoItem` (recycled — extra site beyond §4, included per user decision Session 5).

Re-read `docs/dearrow-design.md` §3.4–§3.5 (recycled-async pattern) and §4 (hook sites) if revisiting.

**What's implemented (reusable building blocks, all under `app/.../util/dearrow/`):**
- `DeArrowService.getInstance().getBranding(videoId)` → `Maybe<DeArrowBranding>` (RxJava3; hash-bucket cache, dedup; never errors to UI; empty == keep original).
- `DeArrowTitleFormatter.selectTitle(branding, autoFormat)` → replacement `String` or `null`.
- `DeArrowSettings.isTitleReplacementEnabled(ctx)` / `isAutoFormatTitlesEnabled(ctx)` / `isEnabled(ctx)`.
- **`DeArrowTitleApplier`** — `apply(TextView titleView, int serviceId, String url)`. Encapsulates the per-site mutable bound-id + `Disposable`; does dispose-on-rebind, the YouTube/pref gate, the video-ID parse, and the stale-result guard. **Reuse this for unit C** (one instance per view site). Already wired into the recycled holders `StreamInfoItemHolder` + `StreamMiniInfoItemHolder`.
- YouTube gate: `serviceId == ServiceList.YouTube.getServiceId()`. Video-ID: `YoutubeStreamLinkHandlerFactory.getInstance().getId(url)` — **throws `ParsingException` AND unchecked `IllegalArgumentException`** on bad URLs; `DeArrowTitleApplier` already multi-catches both.

**Unit C — non-recycled title sites** (give each its own `DeArrowTitleApplier` instance; the applier already disposes the prior fetch on each call):
- `fragments/detail/VideoDetailFragment.java:1808` & `:1840` (`binding.detailVideoTitleView.setText(title)`) — **override the TextView ONLY; never reassign the `title` field** (set at :1690; used by share/notification/history).
- `player/Player.java:3535` (`binding.titleTextView.setText(info.getName())`).
- `info_list/dialog/InfoItemDialog.java:59` (`titleView.setText(info.getName())`).
- `player/PlayQueueActivity.java` queue-item title — design said ~:492 but **NOT confirmed** (only the action-bar title at :81 surfaced). **Rediscover** the queue-item bind site (likely a `PlayQueueItemBuilder`/holder) before editing.
- Line numbers were current at the pre-squash client; the squash changes no content, but always re-grep before editing.

**Then close Phase 4:** manual device/emulator verification → commit on client `patch` → bump the meta `PipePipeClient` gitlink to the new client HEAD and commit the meta. **Push ordering for gitlink bumps: push the client `patch` BEFORE the meta `patch`**, else a recursive clone can't resolve the gitlink in the window between.

**Gotchas:**
- Checkstyle runs over `src/main` + `src/test` (not in `assembleDebug`): 100-col, `final` params/locals/for-vars, no unused imports, no trailing whitespace, trailing newline. Hand-check new files (`grep -nE '.{101,}'`).
- An emoji PostToolUse hook blocks edits to files containing emoji (e.g. a pre-existing donation string in `strings.xml`). The edit still APPLIES; ignore for pre-existing emoji, but don't introduce new ones (use `[X]`/`CAUTION:` etc.).
- `getBranding` is a `Maybe` — use 2-arg `subscribe(onSuccess, onError)` for RxJava lint.

**Fresh-clone setup (if not in this workspace):**
1. `git clone --recursive git@github.com:HritwikSinghal/PipePipe.git && cd PipePipe && git checkout patch && git submodule update --init` — client lands at gitlink `51cd432d5` (the DeArrow work, pushed). Extractor stays upstream/dormant.
2. Optional (work on a branch, not detached): `cd PipePipeClient && git checkout patch`.
3. Build (first run ~10–40 min, still un-run): from meta root `nix run .#build`. Unit tests: `cd PipePipeClient && nix develop -c ./gradlew :app:testDebugUnitTest --tests 'org.schabi.newpipe.util.dearrow.*'` (expect `DeArrowTitleFormatterTest` 16 + `DeArrowResponseParserTest` 7 green).

## Plan

### Phase 1: Foundation — Nix toolchain, branching model & submodule wiring
- [x] Fix malformed `upstream` remote URL (now `https://github.com/InfinityLoop1308/PipePipe.git`)
- [x] Initialize & check out submodules (client + extractor upstream-pinned)
- [x] Inspect PipePipeClient build — composite build (`includeBuild('../PipePipeExtractor')`); AGP 7.3.0 / Gradle 7.5 / Kotlin 1.7.20; compileSdk 33, Java 11; prebuilt `ffmpeg-kit.aar` (no NDK); existing `packageSuffix` release hook
- [x] Write `flake.nix` — Android SDK 33 + build-tools 30.0.3/33.0.1 + JDK 11 + aapt2 override + dev shell + `.#build` app; `nix eval`-validated
- [x] Baseline build under Nix (`nix run .#build`) — **BUILD SUCCESSFUL, 5 debug APKs** (Session 6). Built *with* the uncommitted Phase-4 Unit-C edits, so it also compile-verified that code. Required swapping the JDK to prebuilt Temurin (see Session 6 note).
- [x] PipePipeClient fork wired (`origin`=fork, `upstream`=InfinityLoop1308); `patch` branch created & pushed
- [x] Meta `patch` branch off `main`; `.gitmodules` cleaned + repointed to fork; foundation committed & pushed

### Phase 2: Map SponsorBlock & design DeArrow integration
- [x] Trace SponsorBlock (extractor-side `SponsorBlockExtractorHelper`; settings template = `sponsor_block_settings.xml` + keys + fragment + registry/`main_settings`)
- [x] Identify title/thumbnail render sites + image loader (**Picasso** via `PicassoHelper`); models immutable → replace at view layer (mapped in `docs/dearrow-design.md` §4)
- [x] Research DeArrow API (branding `…/api/branding/<hashPrefix>`, thumbnail generator; GPL-3.0 + attribution required)
- [x] Write `docs/dearrow-design.md` blueprint

### Phase 3: DeArrow service in the Client  *(revised — was "in the Extractor")*
- [x] `DeArrowService` singleton: `Maybe<DeArrowBranding> getBranding(videoId)` via hashPrefix bucket over `NewPipe.getDownloader()` (RxJava3, `Schedulers.io()`)
- [x] POJOs (`DeArrowBranding`/`DeArrowTitle`/`DeArrowThumbnail`) + pure `DeArrowResponseParser` (nanojson)
- [x] Hash-bucket `LruCache` (positive+negative, 6h TTL) + per-prefix in-flight dedup; graceful empty-on-error
- [x] `DeArrowTitleFormatter` (pure) + unit tests (16 cases) — **executed green standalone** (java-17 + cached JUnit)
- [x] No extractor changes; committed & pushed on client `patch`

### Phase 4: DeArrow titles in the Client
- [x] DeArrow settings (master/titles/auto-format toggles) mirroring SponsorBlock prefs + `DeArrowSettings` reader (unit A)
- [x] Wire replacement-title fetch + apply into recycled list holders via `DeArrowTitleApplier` (unit B)
- [x] Wire the non-recycled title sites (detail/player/dialog/queue) — unit C *(also covered the recycled play-queue list, see Decisions; compiled & committed `8ed625702`)*
- [x] Per-item indicator — no pre-existing "show original titles" setting exists (only `show_original_time_ago`, about timestamps), so added a settings-gated "Mark replaced titles" icon instead (default ON; `CenteredImageSpan` of `ic_stars` at the single applier chokepoint). Design in `dearrow-design.md` §7; committed `d6e6bcaa8`.
- [x] Manual on-device verification — **done (Session 8)**: user installed the `PipePipe+` release APK via the GitHub release + Obtainium and verified the de-clickbait titles + marker on a real device.
- [x] Commit & push on client `patch`; bump pinned SHA in meta `patch` *(marker = client `d6e6bcaa8`)*

### Phase 5: DeArrow thumbnails + interactive toggle marker  *(scope EXPANDED Session 6 — see the "Phase 5 scope EXPANDED" note in Decisions)*
- [x] **Design first (brainstorm)** — the marker becomes interactive + always-present; this revises the Phase-4 marker (committed `d6e6bcaa8`). Resolve the open tensions (marker placement/clickability; unified per-item controller) before coding. *(Done Session 7: design = `dearrow-design.md` §8 — hybrid marker [static title star + interactive thumbnail-corner badge], unified `DeArrowItemController`. Full implementation plan written: `docs/superpowers/plans/2026-05-31-dearrow-thumbnails-toggle-marker.md`, 13 tasks. Both uncommitted.)*
- [x] Thumbnail selection: pure, testable `DeArrowThumbnailSelector` (mirrors `DeArrowTitleFormatter`: drop original, prefer locked, then top non-negative votes) + `DeArrowThumbnailUrl` builder (`https://dearrow-thumb.ajay.app/api/v1/getThumbnail?videoID=&time=`); `randomTime*videoDuration` fallback when no explicit timestamp. *(POJOs + parsing already exist from Phase 3.)*
- [x] `dearrow_replace_thumbnails` toggle + `DeArrowSettings.isThumbnailReplacementEnabled`; no-flicker Picasso load (original first; replace with `.noFade()`; 204/error → keep original; no placeholder/error drawable so failures don't clear the original).
- [x] Thumbnail replacement at **lists + video detail**. **Player thumbnail: include only if cheap; OK to SKIP if it needs MediaSession hooking** (bitmap drives notification/lockscreen/end-screen). Player *title* already done (Phase 4 Unit C).
- [x] **Interactive, always-present marker** — when DeArrow on, show the marker on every YouTube item: full opacity when a replacement exists, **faded when the video is absent from the DeArrow DB** (original shown). Tap toggles original <-> DeArrow for **both title and thumbnail together**; tap again reverts. (Replicates the DeArrow browser extension.)
- [x] Verify across feed/search/detail (+ player if included); on-device via release + Obtainium — **done (Session 8)**: user verified thumbnails + the interactive toggle badge on-device.
- [x] Commit & push on client `patch`; bump pinned SHA in meta `patch` *(client recomposed to 3 commits `a860ce2a4..754992090`, force-pushed; meta gitlink bumped this commit)*

### Phase 6: Build config — versioning, identity, signing, `nix run .#build`
- [x] CI-injected `-PforkVersionName`/`-PforkVersionCode` into client `build.gradle` (additive end-of-file; upstream version lines untouched, rebase-safe; per-ABI versionCodeOverride still applies)
- [x] `keystore.properties` → `signingConfigs.release`; `applicationIdSuffix ".plus"` (=> `…NewPipeEnhanced.plus`) + app_name "PipePipe+"
- [x] Generate release keystore (PKCS12, alias `fork`, RSA-4096); set `KEYSTORE_BASE64`/`KEYSTORE_PASSWORD`/`KEY_PASSWORD` secrets on the meta repo *(keystore + passwords backed up at `~/.pipepipe-fork-keystore/`, not in git)*
- [x] `flake.nix` `.#build` now builds the SIGNED release (`:app:assembleRelease` + `keystore.properties` + PipePipe+ identity); added `.#debug` for the fast debug-key loop
- [x] Verify `nix run .#build` produces a signed PipePipe+ APK locally — 5 signed release APKs (apksigner: `CN=PipePipe Plus Fork`; id `…plus`; label `PipePipe+`), BUILD SUCCESSFUL
- [x] Commit build changes on client `patch` (`2f528808c`); bump pinned SHA in meta `patch` (`7877e07`)

### Phase 7: Release workflow on `patch` & default branch
- [x] Add `.github/workflows/release.yml` (workflow_dispatch, recursive submodule checkout, JDK 11 Temurin, decode keystore, build signed APK, gh-release) — in the meta repo (composite build needs both submodules)
- [x] Adapt version computation (`<base>-plus.<run>` / `<base>+<run>` from `build.gradle`); removed continuum's `-x lintVitalRelease` (PipePipe sets `lint.checkReleaseBuilds=false`, so that task doesn't exist)
- [x] Set `patch` as the default branch on origin (done Session 3, early — `workflow_dispatch` UI needs the workflow on the default branch)
- [x] Trigger a test release run; confirm signed APK published — run `26714080846` SUCCESS, published `PipePipe+ 5.1.1-plus.1` (tag `plus-v5.1.1-plus.1`) with 5 signed per-ABI APKs
- [x] Verify APK installs alongside official PipePipe — **done (Session 8)**: user installed the `…NewPipeEnhanced.plus` release alongside the official app on a real device.

### Phase 8: Upstream-sync maintenance & docs
- [ ] Document/script the sync flow: rebase meta `patch` on upstream `main`; rebase submodule `patch` on its upstream; re-pin SHAs
- [ ] Add maintenance notes to README/CLAUDE.md
- [ ] Optional: scheduled CI to detect upstream updates
- [ ] Final end-to-end verification (build + install) and wrap-up

## Status Summary
| Phase | Status | Progress |
|-------|--------|----------|
| Phase 1: Foundation — Nix toolchain, branching & submodule wiring | Done | 7/7 |
| Phase 2: Map SponsorBlock & design DeArrow | Done | 4/4 |
| Phase 3: DeArrow service in the Client | Done (gradle test run pending) | 5/5 |
| Phase 4: DeArrow titles in Client | Done (on-device verified) | 6/6 |
| Phase 5: DeArrow thumbnails + interactive marker | Done (on-device verified) | 7/7 |
| Phase 6: Build config — versioning, identity, signing, nix build | Done (signed release via CI + local nix .#build) | 6/6 |
| Phase 7: Release workflow & default branch | Done (release published; installed on-device) | 5/5 |
| Phase 8: Upstream-sync maintenance & docs | Pending | 0/4 |

## Decisions & Notes
- **2026-06-01 (Session 14) — build-time improvements (CI + local).** Reduced packaging/signing + recompile cost without touching any app source. **(1) `gradle.properties` (client, additive):** `org.gradle.parallel=true` + `org.gradle.caching=true`, heap `-Xmx2048M`→`-Xmx4096M` (avoids GC thrash under parallel workers). Configuration cache was **already enabled upstream** (`org.gradle.unsafe.configuration-cache=true`), so that `/btw` lever needed nothing. **(2) ABI gating — new `-PforkAbiFilter` (client `app/build.gradle`, additive end-of-file, same rebase-safe pattern as the `forkVersion` injection — reconfigures `android.splits.abi` after the `android{}` block so it never edits upstream's `splits{}` DSL; AGP reads splits in afterEvaluate so end-of-script reconfig is honoured):** `=universal` disables per-ABI splitting → **one fat APK** instead of 5 (4 per-ABI + universal), and renames the output to keep the `-universal` filename token (`output.outputFileName` insert before the build-type suffix) so Obtainium's APK filter still matches; a comma list (e.g. `arm64-v8a,universal`) instead does `splits.abi.reset()` + `include(*perAbi)` + `universalApk=<list contains universal>`; absent ⇒ upstream's default 5-APK behaviour (vanilla builds unchanged). **(3) `release.yml` (meta):** both `build-release` and `build-debug` pass `-PforkAbiFilter=universal` (user chose universal-only for **both** release and the diagnostic debug APK); top "Speed levers" comment + release-notes prose updated (prose corrected to state the universal APK only carries `arm64-v8a`+`x86_64`, not "all-architecture", since that's all ffmpeg ships). **(4) `flake.nix` (meta):** `.#build` + `.#debug` now pass `-PforkAbiFilter=universal` by default (mirrors CI, far faster locally), overridable via `nix run .#build -- -PforkAbiFilter=arm64-v8a` (Gradle honours the last `-P` value; `"$@"` comes last). **Key finding:** the prebuilt `ffmpeg/ffmpeg-kit.aar` ships native libs for **only `arm64-v8a` + `x86_64`** and there's no `ndk.abiFilters` restriction — so the universal APK's ABI content is identical to the old universal split APK (no regression), and upstream's `armeabi-v7a`/`x86` split APKs were being built **without any ffmpeg native lib** (ffmpeg-dependent playback broken on those targets), so dropping them costs nothing real. **Verified:** `nix develop -c ./gradlew :app:assembleDebug -PforkAbiFilter=universal` → BUILD SUCCESSFUL in ~37s, config-cache stored with **0 problems** (parallel + caching + config-cache interplay clean), exactly **1** output `PipePipe_5.1.1-universal-debug.apk` (60 MB, `unzip -l` shows `lib/arm64-v8a` + `lib/x86_64` only), dearrow unit suite **48/48** (UP-TO-DATE, counts read from cached XML). **Committed + pushed:** client `patch` (`gradle.properties` + `app/build.gradle`) pushed **first**, then meta `patch` (`flake.nix` + `release.yml` + `docs/progress.md` + the gitlink bump). Release pipeline triggered via `gh workflow run release.yml -f branch=patch`.
- **2026-06-01 (Session 13) — DeArrow bug hunt (`/bugs`); High+Medium fixes IMPLEMENTED, Low deferred.** Ran the bug-finder agent over the entire `util/dearrow/` package + all wiring sites, then verified each finding against the source (the agent over-rated two as crash/Critical — the controller captures its `titleView` non-null, so the late-write paths leak/waste work but don't NPE). Implemented the 4 High/Medium fixes via **4 parallel subagents, each in its own isolated `PipePipeClient` submodule git worktree** (the meta-repo's worktree isolation can't populate the submodule, so worktrees were created on the client repo directly); the 4 fixes touch disjoint files and were consolidated by `git apply` into the main tree. **H1 (High)** — `Player.deArrowController` was the only title-only site never disposed; `Player.destroy()` now calls `deArrowController.dispose()` (else a fetch resolving after the player closes leaks the RxJava subscription + the player view hierarchy and writes to a torn-down `titleTextView`). **M2 (Medium)** — RecyclerView holders cancelled their in-flight fetch only on rebind; added `disposeDeArrow()` to `StreamInfoItemHolder`/`StreamMiniInfoItemHolder` (siblings under `InfoItemHolder`) + `LocalStatisticStreamItemHolder`/`LocalPlaylistStreamItemHolder` (under `LocalItemHolder`), `onViewRecycled` overrides (super + `instanceof`, grid/card subclasses covered transitively) in `InfoListAdapter`+`LocalItemListAdapter`, and a Groupie `unbind` override in `StreamItem.kt` disposing the `R.id.dearrow_controller` view-tag controller — the `boundVideoId` guard already blocked cross-row writes, so this is leak/wasted-work hardening for the recycle-then-teardown-without-rebind window. **M3 (Medium)** — `DeArrowService.bucket()` had a TOCTOU between `cache.get` and `inFlight.computeIfAbsent` (thread A could start a 2nd fetch after thread B warmed the cache + removed its in-flight entry); closed by re-checking the cache *inside* the atomic `computeIfAbsent` mapping fn, serving a warmed hit as `Maybe.<BucketResult>just(warmed).doFinally(inFlight.remove)` so the placeholder self-removes and can't replay stale data after the 6h TTL. **M4 (Medium)** — `DeArrowResponseParser` defaulted a missing `randomTime` to `0.0`, so a title-only/partial entry with a known `videoDuration` and no winning thumbnail generated a `&time=0` (black-intro) frame instead of keeping the original; parser now defaults `randomTime` to `Double.NaN` and `DeArrowThumbnailSelector.selectTime` guards `duration != null && !Double.isNaN(randomTime)` → returns `NaN` (keep original). **Verified centrally** (worktrees can't build — no `../PipePipeExtractor`): `nix develop /home/hritwik/Projects/PipePipe -c ./gradlew :app:assembleDebug :app:testDebugUnitTest --tests 'org.schabi.newpipe.util.dearrow.*'` → BUILD SUCCESSFUL + dearrow suite **48/48** (was 46; +2 M4 regression tests: `DeArrowThumbnailSelectorTest.missingRandomTime_withDuration_returnsNaN`, `DeArrowResponseParserTest.parsesMissingRandomTimeAsNaN`). **Uncommitted** on client `patch` (13 files: `Player.java`, `InfoListAdapter.java`, `LocalItemListAdapter.java`, `StreamItem.kt`, `StreamInfoItemHolder.java`, `StreamMiniInfoItemHolder.java`, `LocalStatisticStreamItemHolder.java`, `LocalPlaylistStreamItemHolder.java`, `DeArrowService.java`, `DeArrowResponseParser.java`, `DeArrowThumbnailSelector.java` + 2 test files). 4 Low findings deferred → see "Deferred / To verify" (L1–L4). Per the standing gotcha, push client `patch` BEFORE the meta gitlink bump.
- **2026-06-01 (Session 12) — DeArrow perf/reliability fix IMPLEMENTED + on-device VERIFIED.** Implemented the Session-11 approved 3-part fix (debug-log-driven, no TDD per user's call this session). **(1) RC2 fix in `DeArrowService.fetchBucket`:** split the old "any non-200 → negative-cache empty for 6h" into 200 = parse+cache, **404 = the ONLY genuine negative (cache empty)**, everything else (5xx/429/etc) = `throw new IOException(...)` → surfaced, NOT cached. Network/timeout already throw `IOException`; a parse error (`JsonParserException`, not `IOException`) is correctly NOT retried and NOT cached. **(2) RC1 fix in `DeArrowPrefetcher`:** new `MAX_PREFETCH_ITEMS = 25` + `Observable…take(25)` (viewport window instead of the whole 500-item feed) and `MAX_CONCURRENCY` **4→2**. **(3) Bounded retry:** `Maybe…retryWhen` with an `AtomicInteger` attempt counter — transient-only (`error instanceof IOException`), `MAX_RETRIES = 2`, exponential backoff `RETRY_BASE_DELAY_MS<<(attempt-1)` = 500ms→1000ms; after retries exhaust, `onErrorComplete()` → empty + uncached (keep original, refetch later). **Verification method (user's call): runtime logcat, not unit tests** — captured a same-session `DeArrowPerf` baseline, applied the fix, rebuilt `nix run .#debug`, reinstalled on the Pixel 10a, re-captured. **Before → after:** prefetch 500@4 → first-25@2 (confirmed in logs); 5xx ~28% → **~9.5%**; **negative-cached buckets 44 → 0** (RC2 gone — all 44 baseline poisons were transient 5xx, zero 404s); fetch latency median **7.8s → 0.4s** (89%→20% >3s); resolve median **~35ms** (655 warm HITs); retry recovered **44/45** transient failures (1 `FAILED after retries`, left uncached). Full table + honest tail note (server itself still slow — saw a 15s wait to *receive* a 503): **`docs/dearrow-perf-investigation.md` → "Verification (Session 12)"**. Compile clean; dearrow unit suite **46/46** (no regression). **Uncommitted** — `DeArrowService.java` + `DeArrowPrefetcher.java` on client `patch`; awaiting user go-ahead. Per the standing gotcha, push client `patch` BEFORE the meta gitlink bump; open question whether to keep or strip the `DeArrowPerf` instrumentation at commit time.
- **2026-05-31 (Session 11) — DeArrow list latency + detail-page reliability: ROOT CAUSE FOUND, fix approved, NOT yet implemented.** Full report: **`docs/dearrow-perf-investigation.md`**. User reported DeArrow titles/thumbnails update **very slowly** in lists (home feed, recommended-below-player) and **sometimes don't load at all** on the video page (vs. the DeArrow extension nailing it every time). Built + installed an instrumented **`BuildConfig.DEBUG`-gated** debug APK ("PipePipe Debug", `…NewPipeEnhanced.debug`) on a Pixel 10a via `nix run .#debug` + `adb install`, captured `DeArrowPerf` logcat during real use. **Evidence:** `sponsor.ajay.app` returned **~32% 5xx** (78×503, 27×502 vs 219×200) with **median 7.7 s / p90 13 s / max 105 s** branding latency; the prefetcher fired **5× full 500-item feed pages** (≈500 bucket requests each) at concurrency 4. **Root causes:** (RC1) `FeedFragment.handleLoadedState` + the adapter chokepoints prefetch the **entire page** (500 items) on every load → self-inflicted overload of the single host → 5xx + multi-second latency (this is the Session-10 "prefetch" fix backfiring, not helping); (RC2) `DeArrowService.fetchBucket` **negative-caches any non-200 (incl. 502/503) for 6 h** → false "no data" → the reliability miss; no retry compounds it on the one-shot detail page. **Approved fix (all 3, pending implementation):** (a) only cache **404** as no-data, treat 5xx/429/timeout as transient (don't cache); (b) cap prefetch to a **~25-item window** + concurrency **4→2**; (c) **bounded retry w/ backoff** on transient errors. **Uncommitted working tree:** instrumentation in `DeArrowService`/`DeArrowItemController`/`DeArrowPrefetcher`/`PicassoHelper` (client `patch` @ `3f39eb780`) + `release.yml` debug-APK publish step (meta `patch` @ `fe888fb`). Instrumentation is debug-only (compiled out of release) and intentionally kept to verify the fix. **Open:** whether bare `nix run` should default to `.#debug` (currently `.#build`=release; use `nix run .#debug` for the diagnostic build).
- **Branching model:** track upstream `main`; our work lives on `patch` in the meta-repo and the client fork. **Fork only what we modify** — only `PipePipeClient` is forked; the extractor stays upstream-pinned/dormant (its fork was deleted, no unique work); `PipePipe.wiki` is never forked/initialized. `patch` is the default branch on both the meta fork and the client fork.
- **DeArrow = client-only, display-time.** Unlike SponsorBlock (extractor-side, player-time, single video), DeArrow de-clickbaits **list items** before you click, so it's a view-layer decoration — resolve a replacement at render time, fall back to the original on any miss. The only extractor dependency is `YoutubeStreamLinkHandlerFactory.getId()` (public API). Scope = **titles + thumbnails** behind toggles, across all YouTube surfaces (lists/detail/player/queue/dialog); local-DB history deferrable.
- **Stack facts:** client uses **RxJava3**, JSON via **nanojson**, images via **Picasso** (its own OkHttp; fetches thumbnails directly — the extractor `Downloader` is text-only). **Never mutate immutable extractor models**, and **never overwrite `VideoDetailFragment.title`** (used by share/notification/history) — replace at the view layer only.
- **Build toolchain = the project's own:** AGP 7.3.0, Gradle 7.5, Kotlin 1.7.20, compileSdk/targetSdk 33, minSdk 21, Java 11. → flake pins **JDK 11** (Gradle 7.5/AGP 7.3 are incompatible with JDK 21) + platform 33 + build-tools 30.0.3 & 33.0.1 + aapt2 override; **no NDK** (the `:ffmpeg` module ships a prebuilt `ffmpeg-kit.aar`). Build runs inside `PipePipeClient/` via its own `./gradlew` (composite-includes `../PipePipeExtractor`). `nix run .#build` currently builds a **debug** APK; Phase 6 switches it to a signed **release** build.
- **Signing / identity:** keystore via **GitHub Secrets** (repo is public — keystore never committed; local signing uses a gitignored auto-generated keystore). App name **"PipePipe+"**, distinct `applicationId` (suffix TBD Phase 6). Release pattern mirrors `~/Projects/reddit/continuum/.github/workflows/release.yml`.
- **Phase 3 verification status:** the POJOs + `DeArrowTitleFormatter` are pure (zero Android/RxJava/nanojson deps) and the formatter's 16 unit tests ran **green standalone**. `DeArrowService` (Android `LruCache` + RxJava3) and the nanojson parser test are verified by review only until the Nix gradle build runs `:app:testDebugUnitTest`.
- **2026-05-31 (Session 5) — flake hardened against JDK 11 daemon crash.** The first real `nix run .#build` crashed the Gradle daemon JVM (SIGSEGV in `ObjectSynchronizer::inflate`, null deref, from a C2-compiled `monitorenter`) under Gradle's 80-thread concurrent dependency download on the Manjaro host. Root cause = OpenJDK 11 biased-locking race (deprecated/removed in later JDKs via JEP 374), not our code or build config. Fix: added a shared `extraJavaToolOptions = "-XX:-UseBiasedLocking"` and exported it via `JAVA_TOOL_OPTIONS` (inherited by the forked daemon JVM; `GRADLE_OPTS` only reaches the client JVM) in both `mkBuildApp` and `mkDevShell`. Mitigation not yet confirmed by a successful build. Fallback if it recurs: add `-XX:TieredStopAtLevel=1`, then try a non-Nixpkgs JDK 11 (e.g. Temurin).
- **2026-05-31 (Session 6) — build FIXED; Session-5 diagnosis corrected.** The `-XX:-UseBiasedLocking` mitigation **did not work** — `nix run .#build` crashed again (`hs_err_pid398492` + `400603`, both with the flag confirmed active, `UseBiasedLocking = false`). Reproduced **3/3**, every crash a null deref (`si_addr=0x0`) *inside* `ObjectSynchronizer::inflate(...)+0x320`. The callers differed — interpreter (`InterpreterRuntime::monitorenter`) **and** C1 (`Runtime1::monitorenter`) — so it was **not** "a C2-compiled monitorenter" and **not** biased locking; the fault is in `inflate` itself. **Real root cause:** nixpkgs-unstable's `jdk11` is **source-built with gcc 15.2.0** (per the crash header), which miscompiles OpenJDK 11's HotSpot lock code; no JVM flag or Gradle setting can fix a miscompiled binary. **Fix:** flake now uses the **prebuilt `pkgs.temurin-bin-11`** (11.0.31, vendor binary built with an OpenJDK-11-era toolchain); removed the dead `extraJavaToolOptions`/`JAVA_TOOL_OPTIONS` plumbing. **Verified:** changing *only* the JDK → BUILD SUCCESSFUL in 6m48s, then `nix run .#build` end-to-end → 5 APKs (`app/build/outputs/apk/debug/PipePipe_5.1.1-{arm64-v8a,armeabi-v7a,x86,x86_64,universal}-debug.apk`). **Committed & pushed:** client `8ed625702` (Unit-C), meta `406c533` (this build fix). Observed upstream `applicationId` = `InfinityLoop1309.NewPipeEnhanced` (debug suffix `.debug`) — note for the Phase-6 PipePipe+ distinct id.
- **2026-05-31 (Session 6) — "Mark replaced titles" indicator (Phase 4 close).** Investigation found **no pre-existing "show original titles" behavior** to respect — the only `show_original_*` pref is `show_original_time_ago` (timestamps), so that half of the plan item was a false premise. Designed + user-approved a settings-gated marker instead (`dearrow-design.md` §7): a small leading `ic_stars` icon on replaced titles, tinted to the **title's own text colour** (chosen over a theme accent so it stays visible in dark mode), via a new `CenteredImageSpan` (minSdk 21 lacks `DynamicDrawableSpan.ALIGN_CENTER`). Implemented at the **single `DeArrowTitleApplier` chokepoint**, so all surfaces inherit it; new pref `dearrow_mark_replaced_titles` (default ON, depends on `dearrow_replace_titles`). Purely visual — never touches the `title` field / notifications / share. Committed `d6e6bcaa8` (client). **Manual on-device verification will be done via the release pipeline + Obtainium**, so it's deferred behind Phase 6/7 rather than local adb.
- **2026-05-31 (Session 6) — Phase 5 scope EXPANDED by user; marker becomes interactive (DESIGN before coding).** New requirements to replicate the DeArrow browser extension's on-YouTube behaviour — captured for next-session design, **NOT yet designed or built**:
  1. **Always-present marker, faded when no data.** With DeArrow on, show the small marker on *every* YouTube item — full opacity when a DeArrow title/thumbnail replacement exists, **faded/dimmed when the video is absent from the DeArrow DB** (original title + thumbnail shown). NOTE: this revises the committed Phase-4 marker (`d6e6bcaa8`), which only appears on replaced titles and is non-interactive.
  2. **Tap-to-toggle original <-> DeArrow, for title AND thumbnail together.** Tapping the marker temporarily reverts to the original title *and* thumbnail; tapping again restores the DeArrow versions.
  3. **Thumbnail sites:** lists + video detail for sure. Player/now-playing thumbnail = include only if cheap; **OK to SKIP DeArrow thumbnails in the player if it requires hooking MediaSession** (the bitmap also drives notification/lockscreen/end-screen). Player *title* replacement is already done (Phase 4 Unit C).
  - **Design tensions to resolve first (brainstorm next session):** (a) **Marker placement/clickability** — a `ClickableSpan` icon inside the title TextView fights the row's open-video tap; the extension overlays the icon on the **thumbnail corner** (a natural tap target). Likely move the marker to a small overlay `ImageView` on the thumbnail rather than the title `ImageSpan`. (b) **Unified per-item controller** — toggling title+thumbnail together needs one object per holder/site holding {original title, DeArrow title, original thumb URL, DeArrow thumb time/URL, toggle state, view refs}; this argues for merging `DeArrowTitleApplier` + the planned thumbnail applier into a single `DeArrowItemController` that fetches branding once and renders both + the marker + the toggle. (c) **Recycling** — marker state + toggle must reset on rebind (extend the `boundVideoId` guard). (d) Every YouTube item already calls `getBranding` (cached), so the faded-vs-full decision is free.
- **2026-05-31 (Session 8) — Phase 5 IMPLEMENTED (subagent-driven, then history recomposed).** Executed the 13-task plan (`docs/superpowers/plans/2026-05-31-dearrow-thumbnails-toggle-marker.md`) via fresh per-task subagents with spec-compliance review + build gates. Built the unified **`DeArrowItemController`** (renamed from `DeArrowTitleApplier`): a 3-arg title-only `apply` keeps player/dialog/queue byte-identical, a 7-arg `apply` (title + thumbnail + badge) drives lists + detail from one branding fetch. Two pure TDD units — **`DeArrowThumbnailSelector`** (NaN sentinel = keep original) + **`DeArrowThumbnailUrl`**. No-flicker `PicassoHelper.loadDeArrowThumbnail` (`noFade`, no placeholder/error). New **`dearrow_replace_thumbnails`** toggle depends on the **MASTER** toggle (not the titles sub-toggle); default ON via XML `defaultValue` + `getBoolean(key,true)` — **NOT** `NewPipeSettings` (§8.7 corrected, the existing four toggles work the same way). Hybrid badge: static title star (kept) + always-present thumbnail-corner badge (faded+non-clickable until data resolves; tap toggles title+thumbnail together). **Decisions baked in:** (a) the selector drops `original` submissions and falls back to a random frame, so a video whose ONLY thumbnail vote is "original" still gets a random-frame replacement — a one-line tunable documented in the selector Javadoc; (b) **player thumbnail skipped** (would need MediaSession hooking; §8.9); (c) the detail title is no longer marked during the loading phase (plain original until data resolves, then title+thumbnail+badge marked in one shot — flicker-free refinement over Phase 4). **Tests:** dearrow unit suite **40/40** (TitleFormatter 16, ResponseParser 7, ThumbnailSelector 11, ThumbnailUrl 6). **History:** the 10 per-task commits were recomposed via the git-rewrite skill (isolated worktree, byte-identical net diff verified) into **3 logical commits `a860ce2a4..754992090`** and **force-pushed** to `origin/patch` (`--force-with-lease`). **Caveat:** the full `nix run .#build` APK gate was interrupted and NOT re-run this session — per-task `assembleDebug` + the unit-test run all passed, but a clean full build + the on-device check remain (gated on Phase 6/7).
- **2026-05-31 (Session 8) — Phase 6 + 7: signed release pipeline LIVE (modeled on `~/Projects/reddit/continuum`).** **Phase 6 (client `build.gradle`):** `signingConfigs.release` loaded from a gitignored `keystore.properties`; release buildType gets `applicationIdSuffix '.plus'` (=> `InfinityLoop1309.NewPipeEnhanced.plus`, installs alongside the official app) + `app_name "PipePipe+"` + `signingConfig` when the keystore is present; additive `-PforkVersionName`/`-PforkVersionCode` injection at end-of-file so upstream version lines are never edited (rebase-safe) — the per-ABI `versionCodeOverride` still applies on top (arm64 release => versionCode 110104 from base 1101). Generated a **PKCS12** fork keystore (alias `fork`, RSA-4096, 10000-day); secrets `KEYSTORE_BASE64`/`KEYSTORE_PASSWORD`/`KEY_PASSWORD` set on the **meta** repo; keystore + password backed up at `~/.pipepipe-fork-keystore/` (NOT in git). **Two gotchas hit + fixed:** (1) JDK 11 keytool defaults to PKCS12, which requires store==key password — separate passwords gave `Get Key failed: Given final block not properly padded`; regenerated with ONE password for both. (2) Removed continuum's `-x lintVitalRelease` — PipePipe sets `lint.checkReleaseBuilds=false`, so that task doesn't exist and excluding it errors (`Task 'lintVitalRelease' not found`). **Phase 7 (`.github/workflows/release.yml`, meta repo, `workflow_dispatch`):** lives in the meta repo (the composite `includeBuild('../PipePipeExtractor')` needs `submodules: recursive`); **JDK 11 Temurin** (NOT 21 — AGP 7.3/Gradle 7.5 incompat; Temurin avoids the gcc-15-miscompiled source JDK); computes `versionName=<base>-plus.<run>` / `versionCode=<base>+<run>` from `build.gradle`, decodes the keystore from secrets, runs `cd PipePipeClient && ./gradlew :app:assembleRelease`, publishes a GitHub Release. `version_name`/`branch` inputs flow through `env:` (no shell-injection). **Verified end-to-end:** local signed `assembleRelease` -> 5 APKs (apksigner `CN=PipePipe Plus Fork`, id `…plus`, label `PipePipe+`); CI debug run green (`26713706137`); **release run `26714080846` SUCCESS -> published `PipePipe+ 5.1.1-plus.1` (tag `plus-v5.1.1-plus.1`) with 5 signed per-ABI APKs.** Client `2f528808c`, meta `7877e07`. **Still open:** `nix run .#build` still emits a *debug* APK (release signing is via CI, not the nix build); on-device install-alongside check pending (Obtainium). Minor: the workflow's actions emit a Node-20 deprecation warning — bump action majors before mid-2026.
- **2026-05-31 (Session 8 addendum) — on-device verified + polish.** User installed the `PipePipe+` release APK (Obtainium) and confirmed on a real device: de-clickbait titles + marker, community thumbnails + interactive toggle badge, and install-alongside the official app (distinct `.plus` applicationId). Polish: (1) `flake.nix` `.#build` repointed to the **signed release** (`:app:assembleRelease` + `keystore.properties`; verified -> 5 signed APKs) and a `.#debug` app added for the fast debug-key loop; (2) `release.yml` action majors bumped off Node-20 (`checkout@v6`, `setup-java@v5`, `action-gh-release@v3`; `wrapper-validation@v4` kept — current major); (3) `.superpowers/` runtime scratch gitignored, the Phase-5 plan doc tracked; (4) keystore backed up to a portable bundle (`~/pipepipe-fork-keystore-backup.tar.gz`) — move it off-machine. This closes Phases 4-7; only Phase 8 (upstream-sync maintenance) remains.
- **2026-05-31 (Session 9) — bugfix: DeArrow now applies on the "What's New" feed (missed by Phase 5).** User reported no de-clickbait titles/thumbnails on the home-screen "What's New" tab. **Root cause:** that feed renders via Groupie's `local/feed/item/StreamItem.kt` (`BindableItem`), a rendering path entirely separate from the `InfoListAdapter`/`StreamInfoItemHolder` + `StreamMiniInfoItemHolder` holders where `DeArrowItemController` was wired in Phases 4–5. `StreamItem.bind()` set `stream.title` + loaded `stream.thumbnailUrl` directly and never invoked DeArrow — so Phase 5's "verify across feed" item was, in fact, never exercised on the real feed (only the search/related lists, which use the holders). The feed layouts already contain the `dearrow_badge` view (shared `list_stream_item`/grid/card) and `StreamEntity` already exposes `serviceId` + `url`, so only the controller call was missing. **Fix:** wired `DeArrowItemController` into `StreamItem.bind()`, mirroring `StreamInfoItemHolder` (7-arg `apply`: title + thumbnail + badge). **Recycling subtlety:** Groupie `StreamItem` is a per-video *data class*, but its underlying view is recycled across videos — a controller stored as an item field would let a late async fetch write onto the wrong row. So the controller is stored **per recycled view** via a view tag (`R.id.dearrow_controller`, new `res/values/ids.xml`), reproducing the holder's one-controller-per-view ownership so the `boundVideoId` guard stays correct. `StreamItem` is used only by the feed (`FeedFragment`/`FeedState`/`FeedViewModel`), so blast radius = the feed only. **Also fixed (same session, at user request):** the two other local stream lists that share the `LocalItemListAdapter` path (a third path, untouched by Phases 4–5) — `local/holder/LocalStatisticStreamItemHolder.java` (watch history / most-played) and `local/holder/LocalPlaylistStreamItemHolder.java` (local playlists). Each got a per-holder `DeArrowItemController` + `dearrow_badge` lookup mirroring `StreamInfoItemHolder`; their grid/card subclasses inherit `updateFromItem` so wiring the base holders covers them. The statistic layouts already had the badge; the playlist NORMAL layout (`list_stream_playlist_item`, a `RelativeLayout`) did not, so the badge was added there for parity. So DeArrow now covers every stream surface: search/related (holders, Phase 4–5) + feed + local playlists + history. **Verified:** `:app:compileDebugKotlin` + `:app:assembleDebug` BUILD SUCCESSFUL; dearrow unit suite 40/40. On-device re-check pending (user's usual release+Obtainium flow). **Committed** on client `patch`: `f2ed4fedf` (feed) + `be766d14c` (local lists); meta gitlink bumped this commit. **Not yet pushed** (awaiting user go-ahead; per the gotcha, push the client `patch` BEFORE the meta `patch`).
- **2026-05-31 (Session 10) — perf fix: DeArrow now prefetches a page ahead of bind (fixes the "slow everywhere except channel" + "top 5 fast, rest lag on scroll" reports).** **Root cause (systematic-debugging):** the only caller of `getBranding` is `DeArrowItemController.apply()`, invoked at RecyclerView **bind time**, with **no look-ahead prefetch anywhere** (grep-confirmed). So beyond the first screenful, every item only *starts* its branding fetch when it scrolls into view, then pays a full network round-trip + (for thumbnails) a cold server-side generation at `dearrow-thumb.ajay.app` + a wait in Picasso's small (~3-4) thread pool. Both the title and the thumbnail gate on that one fetch (user confirmed *both* lag). All list surfaces (channel/search/related/feed) share this identical lazy path and the same holders — so there is **no code-structural reason** the channel page is faster; the difference is runtime/view-mode (user confirmed channel = list/compact, slow surfaces = grid/large: grid shows more + larger + colder thumbnails). The downloader uses **synchronous** `execute()` on unbounded `Schedulers.io()`, so OkHttp's per-host cap is NOT the limiter and the service has no serialization bug — the fetches are genuinely concurrent, they just **start too late**. **Fix:** new **`DeArrowPrefetcher`** (`util/dearrow/`) warms a whole loaded page's branding **and** thumbnails *before* the rows bind — bounded to **4** concurrent (`Observable.flatMap(..., false, 4)`; note `flatMapMaybe` has no maxConcurrency overload, hence `.toObservable()`), off the main thread (`subscribeOn(io)`), YouTube/settings-gated, fire-and-forget (holds no view/Activity refs -> no leak). On each branding success it calls **`PicassoHelper.prefetchDeArrowThumbnail`** = `load(url).transform(transformation).fetch()` — the **same transform** as `loadDeArrowThumbnail`, so the cache key matches and the bind-time `.into(view)` is a cache hit (no flicker, no second round-trip). Wired at the page-load chokepoints: **`InfoListAdapter.addInfoItemList` + `setInfoItemList`** (covers related/search/channel), **`FeedFragment.handleLoadedState`** (What's New; maps `StreamItem -> StreamEntity.toStreamInfoItem()`), and **`LocalItemListAdapter.addItems`** (watch history / most-played / local playlists; a `streamEntityOf` helper maps `StreamStatisticsEntry`/`PlaylistStreamEntry -> StreamEntity.toStreamInfoItem()`, skipping non-stream playlist items). Thread-safety: `getBranding` is now called from io (prefetch) and main (controller) concurrently — safe (`LruCache` synchronized, `inFlight` is `ConcurrentHashMap`, `computeIfAbsent` dedups so a prefetch + a bind for the same bucket share one fetch). **NOT bundled:** the pre-existing upstream main-thread `historyRecordManager.loadStreamState(...).blockingGet()` in `StreamInfoItemHolder.updateFromItem` (separate perf issue). **Tests:** added pure unit tests for the YouTube-id extraction (`DeArrowPrefetcherTest`, 6 cases incl. youtu.be / non-YouTube / non-stream / unparseable / null URL); dearrow suite now **46/46** (was 40/40). **Build:** `:app:assembleDebug` BUILD SUCCESSFUL (full compile -> dex -> package); re-verified after the local-list wiring (assembleDebug + dearrow suite 46/46 still green). Prefetch now covers every stream surface that has DeArrow wiring: search/related/channel + feed + watch history / most-played / local playlists. **Uncommitted** — awaiting user go-ahead; on-device re-check pending (release + Obtainium). Per the gotcha, push client `patch` BEFORE the meta gitlink bump.
- **2026-05-31 (Session 5) — unit C + 2 deviations.** (1) **Recycled play-queue list included:** the queue surfaces titles in *two* places — the non-recycled now-playing title (`PlayQueueActivity.onMetadataUpdate`, the documented §4 ":492" site) AND a *recycled* queue list (`PlayQueueItemBuilder`/`PlayQueueItemHolder`, not in the §4 titles table). Per user decision, included the recycled list too (per-holder applier, unit-B pattern) so the queue matches the de-clickbaited feed. (2) **Null-URL hardening:** added `url != null` to `DeArrowTitleApplier.apply()`'s gate — `YoutubeStreamLinkHandlerFactory.getId(null)` throws an *unchecked* `NullPointerException` (via `new URI(null)`), which the existing `catch (ParsingException | IllegalArgumentException)` did not cover; `VideoDetailFragment.url` is `@Nullable`, so this was a latent crash. Also added `DeArrowTitleApplier.dispose()` for transient sites (wired to the dialog's dismiss). **All review-level only — not yet compiled (Nix gradle build still un-run) or committed.**
- **2026-05-31 (Session 4) — history squashed.** Client: 3 DeArrow commits → 1 (`51cd432d5`). Meta: 8 commits → 2 (build + docs). Both force-pushed; `backup/pre-squash-s4` kept in each repo. Pre-squash content verified byte-identical to the squashed result.

## Blockers
- None.

## Deferred / To verify
- **Baseline build** (`nix run .#build`) — **RESOLVED Session 6.** BUILD SUCCESSFUL, 5 debug APKs. The Session-5 biased-locking theory was wrong; real cause was the gcc-15-miscompiled source JDK, fixed by switching to prebuilt Temurin (see Session 6 note). Crash artifacts deleted Session 6.
- **Phase 3 gradle verification** — **RESOLVED Session 6.** `:app:testDebugUnitTest --tests 'org.schabi.newpipe.util.dearrow.*'` ran green in `nix develop`: `DeArrowTitleFormatterTest` 16 + `DeArrowResponseParserTest` 7 = 23/23. Confirms the nanojson parser + that the service compiles in the full Android classpath.
- **Manual on-device verification of DeArrow titles + marker** — deferred to after Phase 6/7. Plan (user's): publish a signed APK via the GitHub release workflow, install with **Obtainium**, verify on a real device. Marker rendering (`CenteredImageSpan` alignment/visibility across themes) is view glue with no unit coverage, so this is its first real-device check.
- **DeArrow bug-hunt Low findings (Session 13) — documented, NOT yet fixed:**
  - **L1 — toggle-to-original shows the placeholder for blank stored thumbnails.** `DeArrowItemController.render()` (~`:217`) reloads `originalThumbUrl` on badge toggle-off; for local DB items whose stored thumbnail URL is empty/blank this loads the dummy placeholder instead of the real thumbnail. Fix: skip the reload when `originalThumbUrl` is blank (leave the current bitmap), mirroring the no-placeholder DeArrow-URL load.
  - **L2 — `getBranding` in-flight cohort all resolve empty on a transient failure (doc overstates the guarantee).** `DeArrowService.fetchBucket` ends in `onErrorComplete()` shared via `.cache()`, so every subscriber already attached when a bucket fails after retries gets keep-original this pass; only a *later* rebind re-fetches. The cache itself is correctly left empty (self-heals), but the Javadoc (`DeArrowService` ~`:37-40`) implies all views benefit immediately. Fix: correct the Javadoc, or share the error and translate to empty at each call site if the in-flight cohort should also re-fetch.
  - **L3 — `backoffMillis` shift overflow if `MAX_RETRIES` is raised.** `DeArrowService.backoffMillis` (~`:188`) uses `RETRY_BASE_DELAY_MS << (attempt - 1)`; safe at `MAX_RETRIES = 2` but overflows past ~53. Add a `Math.min(..., MAX_BACKOFF)` cap if the retry count ever becomes configurable.
  - **L4 — `toTitleCase` leaves a double space for a bare `>` token.** `DeArrowTitleFormatter.toTitleCase` (~`:90`) yields a stray double space when a token is only `">"` (unlikely in practice — the marker is normally attached to a word). Normalize trailing/duplicate whitespace defensively if desired.
